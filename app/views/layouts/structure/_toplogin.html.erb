<% if user_signed_in? %>
            <ul class="nav navbar-nav navbar-right">
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome, <%= current_user.email %> <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                      <li><%= link_to 'My Articles', '/pages/about' %></li>
                      <li><%= link_to 'My Profile', user_path(current_user) %></li>
                      <li><%= link_to 'Favorites', '/pages/privacy' %></li>
                      <li><%= link_to 'Edit My Profile', edit_user_registration_path %></li>
                      <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
                    </ul>
                  </li>
            </ul>
<% else %>

<% end %>
